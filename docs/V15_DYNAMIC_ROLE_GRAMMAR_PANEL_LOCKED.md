# V15_DYNAMIC_ROLE_GRAMMAR_PANEL_LOCKED

This milestone freezes the V15 dynamic role grammar panel before any 4AKE bridge work.

It is postprocess-only and claim-disabled.

Locked claim:

```text
V15 successfully removed fixed sequence-separation cutoff as a decision rule.
Protein evidence is assigned by dynamic role context, not residue-distance cutoff.
4AKE machine-readable bridge remains pending.
claim_allowed = false
```

The lock is not a protein-folding-solved claim. It records that the partial grammar panel is coherent for the available 1UBQ and 1CLL machine-readable artifacts while 4AKE remains bridge-pending.
